# git-explain

`git-explain` is a lightweight CLI tool written in C++ that explains Git errors in simple, human-friendly language.

Instead of copy-pasting confusing Git error messages, just run:

```bash
git-explain
